import React from 'react';
import { View, Text, Button } from 'react-native';

export default MyWork = ({ navigation }) => {
    return (
        <View>
            <Text>My Work</Text>
            {/* <Button title={'Cleaning Products'} onPress={() => {
                navigation.navigate('CleaningTab');
            }} /> */}
        </View>
    );
}
